{
  "buildCommand": "cd svelte && npm run build",
  "outputDirectory": "svelte/dist",
  "installCommand": "cd svelte && npm install",
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main.py"
    }
  ]
}